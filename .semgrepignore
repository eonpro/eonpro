# Semgrep Ignore File
# False positives and intentional patterns that are safe

# Internal scripts - commands are hardcoded, not user input
scripts/pre-migrate.js
scripts/check-health.js

# Scripts folder - development/build scripts, not production user-facing code
scripts/

# Test files - not production code
tests/
test/
**/*.test.ts
**/*.test.js
**/*.spec.ts
**/*.spec.js

# Migration files - SQL content is intentional
prisma/migrations/

# Build artifacts
.next/
node_modules/
dist/
build/

# False positives - auth middleware sets X-Frame-Options to DENY (secure)
# The middleware is setting a constant value, not user input
src/lib/auth/middleware.ts

# False positives - setTimeout with window.location is for redirect flow
# These are navigating to internal pages, not eval/code execution
src/app/unauthorized/
src/components/PatientIntakeView.tsx
src/components/EditPatientForm.tsx

# Nginx config - the H2C patterns are being mitigated via websocket-only check
docker/nginx/nginx.conf
